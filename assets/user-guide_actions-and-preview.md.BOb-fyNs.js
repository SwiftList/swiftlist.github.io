import{_ as t,o as i,c as o,a2 as n}from"./chunks/framework.BWuWLRhz.js";const p=JSON.parse('{"title":"Actions Menu & Preview","description":"","frontmatter":{},"headers":[],"relativePath":"user-guide/actions-and-preview.md","filePath":"user-guide/actions-and-preview.md","lastUpdated":1784651170000}'),a={name:"user-guide/actions-and-preview.md"};function s(r,e,l,d,c,h){return i(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="actions-menu-preview" tabindex="-1">Actions Menu &amp; Preview <a class="header-anchor" href="#actions-menu-preview" aria-label="Permalink to &quot;Actions Menu &amp; Preview&quot;">​</a></h1><h2 id="actions-menu" tabindex="-1">Actions menu <a class="header-anchor" href="#actions-menu" aria-label="Permalink to &quot;Actions menu&quot;">​</a></h2><p>Every result — file, folder, or app — has a set of actions beyond &quot;just open it&quot;: locate in Explorer, copy path, run as administrator, cut/copy/paste the file itself, delete it (to the Recycle Bin) or permanently delete it, and anything a plugin adds (for example, the full Windows shell right-click menu, cascading submenus included).</p><p>Open it with the <strong>Open actions menu</strong> hotkey (<code>Ctrl+O</code> by default) or the literal Right arrow on a selected result. Inside the menu:</p><ul><li><strong>Next/Previous item</strong> — the arrow keys, or your own configured <a href="./hotkeys.html">next/previous-item hotkey</a>, move the highlight up and down the actions list. A custom binding (even something like a bare <code>Tab</code> key) is honored here exactly the same way it is in the main result list.</li><li><strong>Right arrow / Enter</strong> on an item with a submenu (e.g. a shell cascade menu like &quot;Send to&quot;) drills into it; <strong>Left arrow</strong> or <strong>Backspace</strong> (with an empty search box) backs out one level.</li><li><strong>Escape</strong> exits the actions menu, or clears the search box first if you&#39;d typed something to filter the action list.</li><li>Type to filter the visible actions by name, the same way you&#39;d filter search results.</li></ul><h2 id="quicklook-preview" tabindex="-1">QuickLook preview <a class="header-anchor" href="#quicklook-preview" aria-label="Permalink to &quot;QuickLook preview&quot;">​</a></h2><p>Press the <strong>QuickLook</strong> hotkey (<code>Alt+P</code> by default) on a selected result to open a preview pane docked next to the search window — images, documents, and other previewable file types render without leaving SwiftList. Press it again (or move to a result QuickLook can&#39;t preview) to close it.</p><p>The preview window&#39;s size is fixed and user-configurable — see <a href="./settings/general.html#preview">Settings → General → Preview</a> — and independent of how many results are currently showing. Whatever size you set, SwiftList automatically keeps the preview window fully on-screen: if it doesn&#39;t fit beside the search window on your monitor, it docks to whichever side has room, and if the configured size is larger than your monitor&#39;s usable area, the window is scaled down to fit rather than running off the edge.</p><p>If the file being previewed needs its own native handler to show a popup of its own — most commonly Word or Excel asking for a password on an encrypted document — both the quick window and the preview pane hide themselves for as long as that popup is open, since it would otherwise sit unreachable behind them. This isn&#39;t SwiftList closing or crashing: resolve the popup (enter the password, dismiss it, whatever it&#39;s asking) and both windows come back exactly as you left them, search text and selection included.</p>',9)])])}const w=t(a,[["render",s]]);export{p as __pageData,w as default};
