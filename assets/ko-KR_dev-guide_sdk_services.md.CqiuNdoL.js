import{_ as t,o as d,c as o,a2 as c}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"호스트 서비스","description":"","frontmatter":{},"headers":[],"relativePath":"ko-KR/dev-guide/sdk/services.md","filePath":"ko-KR/dev-guide/sdk/services.md","lastUpdated":1785595777000}'),r={name:"ko-KR/dev-guide/sdk/services.md"};function a(i,e,s,n,l,u){return d(),o("div",null,[...e[0]||(e[0]=[c('<h1 id="호스트-서비스" tabindex="-1">호스트 서비스 <a class="header-anchor" href="#호스트-서비스" aria-label="Permalink to &quot;호스트 서비스&quot;">​</a></h1><p>호스트 앱의 기능을 다시 플러그인에 노출하는, <code>PluginSdk.Services</code>에 있는 정적 서비스들입니다. 각각은 호스트가 시작 시 연결해 두는 델리게이트를 감싸는 얇은 정적 클래스이므로, 플러그인은 그 아래에서 실제로 무엇이 실행되든 항상 동일한 방식으로 호출합니다.</p><table tabindex="0"><thead><tr><th>서비스</th><th>목적</th></tr></thead><tbody><tr><td><code>FuzzyMatchService</code></td><td><code>IsMatch(pattern, text)</code> — <code>text</code>(또는 그 별칭 중 하나)가 fzf 문법의 <code>pattern</code>과 일치하는지, 호스트 자체 검색이 사용하는 것과 정확히 동일한 매칭 로직으로 판단합니다. <code>GetHighlightMask(text, query)</code> — 그 쌍에 대한 문자 단위 하이라이트 마스크로, 호스트 자체 결과가 하이라이트할 때 쓰는 것과 동일한 리터럴/퍼지/별칭 폴백 단계(CJK 병음 포함)를 사용합니다. 이 덕분에 플러그인의 결과도 단순 리터럴 부분 문자열 매칭만 처리하는 대신 일관되게 하이라이트됩니다.</td></tr><tr><td><code>TranslationService</code></td><td>현재 활성화된 언어에 대해 런타임에 조회하는 <code>Get(key)</code> / <code>Format(key, args)</code>. 플러그인 자체의 내장 JSON 번역을 로드하는 <code>LoadEmbeddedTranslations(assembly, cultureKey, typeName)</code>. <code>GetSupportedCultures(assembly)</code>. <code>GetCurrentCulture()</code> — 앱에서 현재 선택된 UI 언어(예: <code>&quot;zh-CN&quot;</code>)로, OS 시스템 로캘과는 독립적인 사용자 설정입니다. 원시 문화권 코드 자체가 필요할 때(예: HTTP <code>Accept-Language</code> 헤더에 넣거나 번역 API의 대상 언어를 고를 때)만 이를 사용하세요 — <code>CultureInfo.CurrentUICulture</code>는 이 설정이 아니라 OS 로캘을 반영하며, 사용자의 Windows 언어와 앱 내 언어가 다를 때는 조용히 이 값과 어긋나게 됩니다.</td></tr><tr><td><code>IconService</code></td><td><code>GetIcon(path, isDir)</code>와 <code>GetThumbnail(path, size)</code> — 캐시된 셸 아이콘/썸네일 추출로, 플러그인이 직접 Windows 아이콘 API를 호출할 필요가 없습니다.</td></tr><tr><td><code>FavoritesService</code></td><td><code>GetFavorites()</code> — 사용자의 <a href="./../../user-guide/settings/favorites.html">즐겨찾기</a> 목록(<code>FavoriteItem</code>: Name, Path)에 대한 읽기 전용 접근입니다.</td></tr><tr><td><code>HistoryService</code></td><td><code>GetHistoryEntries()</code> — 기록된 모든 <a href="./../../user-guide/settings/history.html">기록</a> 항목을 최근에 연 순서로, <code>HistoryEntry { Keyword, Path, Kind, Time }</code> 형태로 반환합니다(<code>Kind</code>는 <code>HistoryEntryKind</code>: <code>File</code> / <code>Folder</code> / <code>Application</code>. <code>Keyword</code>는 그 항목으로 이어진 검색 텍스트이며, 검색어 없이 — 예를 들어 빠른 패널 탭에서 — 바로 연 경우에는 빈 문자열입니다. <code>Time</code>은 유닉스 초 단위입니다). 각 경로는 가장 최근에 그 경로로 이어진 키워드 아래로 한 번만 나타납니다.</td></tr><tr><td><code>FileMetadataService</code></td><td><code>GetMetadataAsync(paths)</code> — 현재 결과 목록에 <strong>이미 포함되어 있지 않은</strong> 경로에 대한 일괄 Size/Created/Modified/Accessed 조회(<a href="./abstractions.html#filemetadata"><code>FileMetadata</code></a>). 모든 <code>ISearchResult</code>는 이미 자체 <code>Metadata</code> 속성을 통해 이를 무료로 제공하므로(<a href="./abstractions.html#isearchresult">공유 추상화</a> 참고), 이 서비스는 다른 방식으로 얻은 경로(예: 자체 설정에서 가져온 경로)에 대해서만 사용하세요.</td></tr><tr><td><code>DirectoryIndexerService</code></td><td><code>RegisterDirectory(pluginId, path, recursive, filterPattern)</code> / <code>UnregisterDirectories(pluginId)</code> / <code>SearchDirectoriesAsync(pluginId, query, token)</code> — 플러그인이 그 메커니즘을 직접 재구현하지 않고도 자체 디렉터리를 백그라운드 인덱싱과 USN 모니터링에 등록할 수 있게 해줍니다. <code>DirectoryChanged</code> 이벤트를 구독하면 등록한 디렉터리가 디스크에서 바뀌었을 때 알림을 받습니다(등록할 때 쓴 <code>pluginId</code>가 함께 오므로 자기 것이 아니면 무시하세요). <code>NotifyDirectoryChanged(pluginId)</code>가 이를 발생시키며, 감시가 반응하면 호스트가 대신 호출해 줍니다. <code>EnumerateDirectoryAsync(path, recursive, filterPattern, limit, token)</code>은 파일 시스템이 아니라 같은 인덱스에서 한 디렉터리의 내용을 나열합니다 — 호스트가 인덱싱하는 드라이브라면 디스크 I/O가 전혀 없고, 인덱싱되지 않은 디렉터리는 실제 순회로 자동 전환되므로 호출자가 어느 쪽인지 판단할 필요가 없습니다. 스트리밍으로 반환되며 <code>filterPattern</code>이 거르는 것은 <strong>파일</strong>입니다(디렉터리는 항상 반환되니 필요 없으면 <code>IsDir</code>로 걸러내세요). 숨김/시스템 항목은 절대 반환되지 않습니다. 재귀 나열에서는 <code>limit</code>을 설정할 값어치가 있습니다 — <code>EnumerateDirectoryAsync(@&quot;C:\\&quot;, recursive: true)</code>는 요청한 그대로 볼륨의 모든 항목을 돌려줍니다.</td></tr><tr><td><code>RecentFilesService</code></td><td><code>GetRecentFilesAsync(directories, limit, maxAgeMinutes, token)</code> — 여러 디렉터리 아래에서 가장 새로운 항목들을 최신순으로. 디스크가 아니라 호스트의 메모리 인덱스가 답합니다. 디렉터리별이 아니라 전체를 <strong>하나로</strong> 병합한 목록입니다. 파일만: 폴더 자신의 수정 시각은 안에서 뭔가 추가·삭제될 때마다 바뀌므로, &quot;무엇을 작업했는가&quot;를 보여 줘야 할 목록의 맨 위에 &quot;작업 중인 폴더&quot;가 올라오게 됩니다. <code>limit</code>에 0이면 개수 제한 없음, <code>maxAgeMinutes</code>에 0이면 기간 제한 없음이지만, 둘 다 두지 않으면 단지 더 새로운 것이 없다는 이유만으로 방치된 폴더가 한 달 전 파일을 계속 내놓습니다. 호스트가 인덱싱하지 않는 디렉터리는 그 자리에서 훑지 않고 그냥 아무것도 기여하지 않습니다 — 여기서 원하는 것은 빠른 답이거나 없거나입니다. 느린 쪽은 <code>DirectoryIndexerService.EnumerateDirectoryAsync</code>가 있습니다.</td></tr><tr><td><code>ExplorerPathService</code></td><td><code>GetLastActivePath()</code> — 탐색기 창이나 파일 대화 상자가 마지막으로 보여 주던 폴더. 한 번도 없었다면 <code>null</code>. 호스트 자신의 창 추적이 채우며, SwiftList 자신의 UI뿐 아니라 <strong>모든</strong> 애플리케이션의 파일 대화 상자를 보고 있으므로 &quot;사용자가 마지막으로 실제 들여다본 폴더&quot;를 뜻합니다 — 플러그인이 스스로 알아낼 수 있는 것이 아닙니다. <a href="./core-search-actions.html"><code>IActivePathCollector</code></a>와는 반대 방향입니다: 그쪽은 서드파티 파일 관리자가 무엇을 보여 주고 있는지 플러그인이 호스트에게 <strong>알려 주는</strong> 것이고, 이쪽은 물어보는 것입니다. 아직 존재한다는 보장은 없습니다: 기록하는 것은 사용자가 갔던 곳이며, 그 폴더는 이미 삭제되었거나 분리되었을 수 있습니다.</td></tr><tr><td><code>PluginSettingsService</code></td><td><code>GetSetting&lt;T&gt;(pluginId, key, defaultValue)</code> — 호스트의 설정 저장소에서 플러그인 자체의 영속화된 설정에 대한 읽기 전용 접근입니다. 세 단계를 순서대로 거칩니다. 사용자가 저장한 적이 있다면 그 영속화된 값, 아무것도 저장된 적이 없다면 해당 필드에 대한 <code>IConfigurable</code> 스키마 자체의 <code>DefaultValue</code>, 그마저도 없다면 마지막 수단으로 여러분이 전달한 <code>defaultValue</code> 인수입니다 — 이렇게 하면 스키마에 선언된 기본값이 단일한 정보원이 되어, 호출부에 하드코딩된 사본을 또 둘 필요가 없습니다. 값을 매번 다시 읽는 대신 캐시한다면, <code>SettingChanged(pluginId, key)</code> 이벤트를 구독하여 여러분의 플러그인에 대해 이 이벤트가 발생할 때 캐시를 폐기하세요 — 호스트는 설정 페이지에서 저장 직후에 이 이벤트를 발생시키며, 이는 무효화하기에 신뢰할 수 있는 유일한 시점입니다(매 키 입력마다, 또는 폴링 방식으로 확인하면 우연히 다음에 무언가가 트리거될 때까지, 혹은 영영 변경을 감지하지 못할 수 있습니다).</td></tr><tr><td><code>SearchRefreshService</code></td><td><code>RefreshIfMatches(queryMatches)</code> — 데이터가 비동기로 도착하는 <code>IInstantResultProvider</code>용입니다(<a href="./core-search-actions.html#iinstantresultprovider"><code>IInstantResultProvider</code></a> 참고). 백그라운드 조회가 끝나고 결과를 캐시한 뒤, 검색의 현재 쿼리 텍스트에 대한 서술자와 함께 이를 호출하면, 호스트가 그 서술자에 일치하는 모든 활성 검색을 다시 실행하여 사용자가 다시 입력할 필요 없이 이제 캐시된 결과가 실제로 나타나게 합니다.</td></tr><tr><td><code>Logger</code></td><td><code>Log(message, level = LogLevel.Info)</code> — App의 로그 파일에 기록하며, <strong>설정 → 서비스 상태 → App</strong>에서 호스트 자체 로그 라인과 완전히 동일하게 보입니다.</td></tr><tr><td><code>PluginPromptService</code></td><td><code>Prompt(title, fields, initialValues?)</code> — 주어진 <a href="./abstractions.html#iconfigurable"><code>PluginConfigField</code></a> 값을 묻는 작은 모달을 표시합니다(<code>IConfigurable</code>의 구성 대화상자가 사용하는 것과 동일한 필드 스키마/렌더링). <code>initialValues</code>(<code>Key</code>로 매칭)나 각 필드 자체의 <code>DefaultValue</code>로 미리 채워집니다. 입력된 값을 필드 <code>Key</code>로 키가 매겨진 형태로 반환하며, 사용자가 취소했다면 <code>null</code>을 반환합니다 — 이 값들은 플러그인의 실제 영속화된 설정에서 읽거나 쓰이는 일이 전혀 없으므로, 실제 설정을 건드리지 않으면서 설정 필드의 스키마를 순전히 일회성 입력(예: &quot;추가하기 전에 이름을 지어주세요&quot;)에 재사용해도 안전합니다.</td></tr></tbody></table><p><code>LogLevel</code>은 <code>Error</code> / <code>Warn</code> / <code>Info</code> / <code>Debug</code>로, <a href="./../../user-guide/settings/service-status.html">서비스 상태 로그 뷰어</a>의 레벨 필터와 일치합니다.</p><h2 id="셸-파일-작업" tabindex="-1">셸 파일 작업 <a class="header-anchor" href="#셸-파일-작업" aria-label="Permalink to &quot;셸 파일 작업&quot;">​</a></h2><p><code>SwiftList.PluginSdk.Shell.FileOperations</code> — Windows 셸 자체의 <code>IFileOperation</code>을 얇게 감싼 것입니다. 플러그인이 파일을 옮길 때 탐색기와 똑같은 진행 대화상자, 똑같은 &quot;파일이 이미 있습니다&quot; 확인, 똑같은 실행 취소 항목이 나옵니다. 미묘하게 다르게 동작하는 <code>System.IO</code> 호출이 아니라요.</p><table tabindex="0"><thead><tr><th>헬퍼</th><th>용도</th></tr></thead><tbody><tr><td><code>ShellPasteHelper</code></td><td><code>PasteAsync(sourcePaths, destinationFolder, move, onCompleted?)</code> — 임의 개수의 경로를 한 폴더로 복사(또는 이동)합니다. <strong>하나의</strong> 셸 작업으로 묶이므로 드라이브를 넘나드는 다중 선택도 파일마다가 아니라 대화상자 하나만 뜹니다. 던져 놓고 바로 반환합니다: 사용자가 열어 둔 채 둘 수 있는 네이티브 대화상자를 기다리며 막으면 호출자만 멈출 뿐이니까요. <code>onCompleted</code>는 작업이 끝났을 때 발생합니다 — 다 복사했든 사용자가 취소했든 상관없이. 대상 폴더를 보여 주는 화면에게 두 경우의 답은 &quot;다시 가서 보라&quot;로 같기 때문입니다.</td></tr><tr><td><code>ShellDeleteHelper</code></td><td><code>DeleteAsync(paths, permanent)</code> — 휴지통으로 보내거나 완전히 삭제합니다. 역시 한 번의 작업, 한 번의 확인으로 묶이며 던져 놓고 반환합니다.</td></tr><tr><td><code>VirtualFileExtractor</code></td><td><code>HasVirtualFiles(dataObject)</code> / <code>Extract(dataObject, targetFolder)</code> — 끌기가 실어 온, 아직 디스크에 없는 파일을 써냅니다: 브라우저에서 끌어온 이미지, 메일 클라이언트에서 끌어온 첨부, zip 미리 보기에서 끌어온 파일. 모두 경로가 아니라서 <code>IDataObject.GetData(DataFormats.FileDrop)</code>으로는 아무것도 얻지 못합니다. 실제로 오는 것은 이름을 나열한 디스크립터와, 인덱스로 하나씩 건네지는 바이트이고, 이 클래스가 그것을 풀어냅니다. 종류로 거르지 않는 것은 의도한 바입니다: 끌기 쪽이 건네려는 것을 거부하려면 확장자를 믿거나 바이트를 들여다봐야 하는데, 둘 다 이 헬퍼가 할 일이 아닙니다. <code>ResolveDestination(folder, name)</code>은 &quot;덮어쓰지 않고 (2)를 붙이는&quot; 같은 이름 규칙으로, 직접 파일을 쓰는 호출자를 위해 공개해 둔 것입니다.</td></tr></tbody></table><p>두 비동기 헬퍼는 SDK 자체의 STA 작업 스레드(<code>ShellOperationStaWorker</code>, 호스트가 시작합니다)에서 돌아갑니다 — 셸의 COM 인터페이스가 STA를 요구하므로, 공유하면 플러그인이 자기 아파트먼트를 띄울 필요가 없습니다.</p>',8)])])}const p=t(r,[["render",a]]);export{g as __pageData,p as default};
