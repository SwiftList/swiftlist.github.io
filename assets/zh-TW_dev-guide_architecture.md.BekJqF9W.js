import{_ as o}from"./chunks/architecture.Cs8uKTTN.js";import{_ as c,o as r,c as i,a2 as a}from"./chunks/framework.BWuWLRhz.js";const u=JSON.parse('{"title":"架構設計","description":"","frontmatter":{},"headers":[],"relativePath":"zh-TW/dev-guide/architecture.md","filePath":"zh-TW/dev-guide/architecture.md","lastUpdated":1785214938000}'),t={name:"zh-TW/dev-guide/architecture.md"};function d(l,e,s,p,n,h){return r(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="架構設計" tabindex="-1">架構設計 <a class="header-anchor" href="#架構設計" aria-label="Permalink to &quot;架構設計&quot;">​</a></h1><p><img src="'+o+'" alt="SwiftList 架構圖"></p><h2 id="處理程序拆分" tabindex="-1">處理程序拆分 <a class="header-anchor" href="#處理程序拆分" aria-label="Permalink to &quot;處理程序拆分&quot;">​</a></h2><p>SwiftList 執行為三個獨立處理程序，按權限層級和生命週期有意拆分:</p><ul><li><strong><code>SwiftList.Service</code></strong> —— 一個以 <code>LocalSystem</code> 身分執行的 Windows 服務。它負責全部檔案索引工 作:讀取 NTFS/ReFS 磁碟的 USN 記錄檔與 MFT、直接走訪並監看其他本機檔案系統(它們沒有記錄檔可 讀)、掃描並快取網路共用，並透過具名管道回答搜尋查詢。在 SYSTEM 層級執行意味著它可以讀取所有使 用者帳戶都被允許看到的原始磁碟區中繼資料，而不需要讓互動式的 App 處理程序取得它本不需要的提升 權限。</li><li><strong><code>SwiftList.App</code></strong> —— 使用者態、Session 層級的 WPF 應用程式:搜尋視窗、設定視窗、熱鍵處理、動 作選單/QuickLook 介面都在這裡。它透過具名管道(<code>Core.Services</code> 裡的 <code>SearchService</code>/ <code>UsnServicePipeServer</code>)和 Service 通訊，從不直接存取磁碟索引。它自己也額外代管了一條按使用者區 分的管道(<code>AppSearchPipeService</code>)，讓 <code>slf</code> 命令列夥伴工具(參見<a href="./../user-guide/cli.html">命令列搜尋</a>) 能重複使用 App 已經初始化好的搜尋狀態——已載入的別名/外掛提供方、已設定的網路磁碟索引——而不用一 個獨立的用戶端處理程序自己重新初始化一遍。</li><li><strong><code>SwiftList.Service --hook</code></strong> —— 一個獨立的小型處理程序，專門代管低層級全域鍵盤鉤子，這樣鉤 子當機或者某個前景應用程式行為異常都不會連累主 App 處理程序。它還會載入外掛的視窗整合轉接器， 並在自己的處理程序裡執行這些呼叫——見下文<a href="#外掛在架構中的位置">外掛在架構中的位置</a>。</li></ul><h2 id="共用的-core" tabindex="-1">共用的 Core <a class="header-anchor" href="#共用的-core" aria-label="Permalink to &quot;共用的 Core&quot;">​</a></h2><p><code>Core</code> 是一個被 Service 和 App 同時參照的類別庫。它包含:</p><ul><li>搜尋引擎(<code>Core/SearchIndex/Fzf/*</code>)—— 一套仿照 <code>fzf</code> 命令列工具演算法實作的模糊比對引擎，配合 一個查詢剖析器(<code>SearchQueryParser</code>)處理磁碟機代號定向和路徑模式搜尋。</li><li>執行階段索引(<code>Core/IndexV2/*</code>)—— 由 USN/MFT 讀取結果建構的記憶體對映欄式快照格式，配合一個 記憶體中的增量覆蓋層記錄快照之後的變更。</li><li>IPC 契約(<code>SearchRequestMessage</code>、<code>SearchResponseBinarySerializer</code> 等)—— App 和 Service 兩邊 完全共用同一份定義，確保雙方對線路格式的理解始終一致。</li><li><code>Logger</code> —— 寫入各處理程序獨立的記錄檔(<code>service.log</code>、<code>app.log</code>、<code>hook.log</code>)，都可以在 App 的 設定 → 執行狀態 記錄檢視器裡讀取(但不是都能直接寫入)。</li></ul><h2 id="外掛在架構中的位置" tabindex="-1">外掛在架構中的位置 <a class="header-anchor" href="#外掛在架構中的位置" aria-label="Permalink to &quot;外掛在架構中的位置&quot;">​</a></h2><p>外掛是參照 <code>PluginSdk</code> 的 <code>.dll</code> 組件，由 App 處理程序載入(見<a href="./getting-started.html">快速上手</a>和 <a href="./packaging.html">封裝與發布</a>)。SwiftList 自帶兩個外掛作為一等公民範例—— <code>SwiftList.Plugins.CoreExtensions</code>(內建檔案動作與 Shell 右鍵選單整合)和 <code>SwiftList.Plugins.PinyinAlias</code>(中文檔名拼音別名)——參見<a href="./examples.html">外掛範例</a>了解這兩個外 掛的詳細走讀。</p><p>外掛從不直接和 Service 通訊;它們透過外掛 SDK 參考裡記錄的介面和 App 互動，如果需要索引自訂目 錄，則透過 <code>DirectoryIndexerService</code> 代為向 Service 轉發請求。</p><p>視窗整合類轉接器是「只在 App 裡跑」這條規則的唯一例外: <a href="./sdk/system-adapters.html"><code>IActivePathCollector</code>、<code>IFileDialogAdapter</code>、<code>IInlineSearchAdapter</code></a> 的實作 會被再載入一份到 Hook 處理程序裡，它們的呼叫實際在 Hook 處理程序裡執行，而不是在 App 裡。這也是為 什麼 SwiftList 能操作一個以系統管理員身分執行的檔案總管/檔案對話方塊/第三方檔案管理員視窗——即使 App 本身從來不提權:Windows 不允許低權限處理程序向高權限處理程序傳送輸入，所以這類呼叫必須從一個 和目標視窗權限層級相同的處理程序發起。</p>',12)])])}const g=c(t,[["render",d]]);export{u as __pageData,g as default};
