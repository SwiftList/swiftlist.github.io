import{_ as a,o,c as d,a2 as t}from"./chunks/framework.BWuWLRhz.js";const m=JSON.parse('{"title":"Servicios del host","description":"","frontmatter":{},"headers":[],"relativePath":"es-ES/dev-guide/sdk/services.md","filePath":"es-ES/dev-guide/sdk/services.md","lastUpdated":1785595777000}'),r={name:"es-ES/dev-guide/sdk/services.md"};function i(s,e,n,c,l,u){return o(),d("div",null,[...e[0]||(e[0]=[t('<h1 id="servicios-del-host" tabindex="-1">Servicios del host <a class="header-anchor" href="#servicios-del-host" aria-label="Permalink to &quot;Servicios del host&quot;">​</a></h1><p>Servicios estáticos en <code>PluginSdk.Services</code> que exponen funcionalidad de la aplicación host a los plugins — cada uno es una fina clase estática que envuelve un delegado que el host conecta al iniciarse, de modo que los plugins los llaman de la misma manera sin importar qué haya funcionando por debajo.</p><table tabindex="0"><thead><tr><th>Servicio</th><th>Propósito</th></tr></thead><tbody><tr><td><code>FuzzyMatchService</code></td><td><code>IsMatch(pattern, text)</code> — si <code>text</code> (o uno de sus alias) coincide con un <code>pattern</code> de sintaxis fzf, usando exactamente la misma coincidencia que emplea la propia búsqueda del host; <code>GetHighlightMask(text, query)</code> — la máscara de resaltado por carácter para ese par, usando los mismos niveles de reserva literal/difuso/alias (incluido el pinyin CJK) con los que el host resalta sus propios resultados, de modo que los resultados de un plugin se resalten de forma coherente en lugar de solo gestionar una coincidencia de subcadena literal.</td></tr><tr><td><code>TranslationService</code></td><td><code>Get(key)</code> / <code>Format(key, args)</code> para búsquedas en tiempo de ejecución contra el idioma activo; <code>LoadEmbeddedTranslations(assembly, cultureKey, typeName)</code> para cargar las propias traducciones JSON incrustadas de un plugin; <code>GetSupportedCultures(assembly)</code>; <code>GetCurrentCulture()</code> — el idioma de interfaz actualmente seleccionado por la aplicación (por ejemplo, <code>&quot;zh-CN&quot;</code>), un ajuste de usuario independiente de la configuración regional del sistema operativo. Recurre a esto solo cuando necesites el propio código de cultura en bruto (por ejemplo, para ponerlo en una cabecera HTTP <code>Accept-Language</code> o elegir el idioma de destino de una API de traducción) — <code>CultureInfo.CurrentUICulture</code> refleja la configuración regional del sistema operativo, no este ajuste, y discrepará en silencio con él siempre que el idioma de Windows del usuario y el idioma dentro de la app difieran.</td></tr><tr><td><code>IconService</code></td><td><code>GetIcon(path, isDir)</code> y <code>GetThumbnail(path, size)</code> — extracción de icono/miniatura del shell con caché, de modo que un plugin nunca tenga que invocar directamente las API de iconos de Windows.</td></tr><tr><td><code>FavoritesService</code></td><td><code>GetFavorites()</code> — acceso de solo lectura a la lista de <a href="./../../user-guide/settings/favorites.html">Favoritos</a> del usuario (<code>FavoriteItem</code>: Name, Path).</td></tr><tr><td><code>HistoryService</code></td><td><code>GetHistoryEntries()</code> — cada entrada registrada de <a href="./../../user-guide/settings/history.html">Historial</a>, con la abierta más recientemente primero, como <code>HistoryEntry { Keyword, Path, Kind, Time }</code> (<code>Kind</code> es un <code>HistoryEntryKind</code>: <code>File</code> / <code>Folder</code> / <code>Application</code>; <code>Keyword</code> es el texto de búsqueda que llevó hasta ella, vacío si se abrió sin escribir ninguna consulta, por ejemplo directamente desde una pestaña del Panel Rápido sin ninguna consulta; <code>Time</code> son segundos Unix). Cada ruta aparece como máximo una vez, bajo cualquiera que sea la palabra clave que más recientemente haya llevado hasta ella.</td></tr><tr><td><code>FileMetadataService</code></td><td><code>GetMetadataAsync(paths)</code> — consulta por lotes de Size/Created/Modified/Accessed (<a href="./abstractions.html#filemetadata"><code>FileMetadata</code></a>) para una ruta que <strong>no</strong> sea ya uno de tus resultados actuales — cada <code>ISearchResult</code> ya lleva esto consigo mediante su propia propiedad <code>Metadata</code> sin coste alguno (ver <a href="./abstractions.html#isearchresult">Abstracciones</a>), así que recurre a este servicio solo para rutas que hayas obtenido de otra manera (por ejemplo, desde tu propia configuración).</td></tr><tr><td><code>DirectoryIndexerService</code></td><td><code>RegisterDirectory(pluginId, path, recursive, filterPattern)</code> / <code>UnregisterDirectories(pluginId)</code> / <code>SearchDirectoriesAsync(pluginId, query, token)</code> — permite que un plugin registre sus propios directorios para indexación y monitorización USN en segundo plano, sin reimplementar esa maquinaria. Suscríbete al evento <code>DirectoryChanged</code> para enterarte de que un directorio registrado cambió en disco (lleva el <code>pluginId</code> con el que se registró, así que ignora los que no sean tuyos); <code>NotifyDirectoryChanged(pluginId)</code> lo dispara, cosa que el host ya hace por ti cuando salta el vigilante. <code>EnumerateDirectoryAsync(path, recursive, filterPattern, limit, token)</code> lista el contenido de un directorio desde ese mismo índice en vez de desde el sistema de archivos: cero E/S de disco en una unidad que el host indexa, y un recorrido real en una que no, de modo que nunca tienes que decidir cuál de los dos casos aplica. Emite en streaming, <code>filterPattern</code> selecciona <strong>archivos</strong> (los directorios vuelven siempre — descártalos por <code>IsDir</code> si no los quieres), las entradas ocultas y de sistema nunca se devuelven, y vale la pena poner <code>limit</code> en un listado recursivo, ya que <code>EnumerateDirectoryAsync(@&quot;C:\\&quot;, recursive: true)</code> entregará todas y cada una de las entradas del volumen, exactamente como se pidió.</td></tr><tr><td><code>RecentFilesService</code></td><td><code>GetRecentFilesAsync(directories, limit, maxAgeMinutes, token)</code> — las entradas más nuevas bajo un conjunto de directorios, lo más reciente primero, respondido desde el índice en memoria del anfitrión y no desde el disco. Los directorios se tratan como una sola lista fusionada, no una por directorio. Solo archivos: la fecha de modificación de una carpeta cambia cada vez que se añade o quita algo dentro, lo que pondría las carpetas en las que estás trabajando en lo alto de una lista pensada para mostrar en qué has trabajado. <code>limit</code> a 0 significa sin tope y <code>maxAgeMinutes</code> a 0 sin corte por antigüedad, pero dejar ambos sin poner hace que una carpeta inactiva siga ofreciendo archivos de hace un mes solo porque no hay nada más nuevo. Un directorio que el anfitrión no indexa no aporta nada en lugar de recorrerse en vivo — aquí se quiere la respuesta rápida o ninguna, y <code>DirectoryIndexerService.EnumerateDirectoryAsync</code> está para la lenta.</td></tr><tr><td><code>ExplorerPathService</code></td><td><code>GetLastActivePath()</code> — la carpeta que una ventana del Explorador o un diálogo de archivos mostraba por última vez, o <code>null</code> si aún no ha habido ninguna. La rellena el propio seguimiento de ventanas del anfitrión sobre los diálogos de archivos de <em>todas</em> las aplicaciones, no solo la interfaz de SwiftList, así que significa la última carpeta que el usuario miró realmente en cualquier sitio: no algo que un plugin pudiera deducir por su cuenta. Es la dirección contraria a <a href="./core-search-actions.html"><code>IActivePathCollector</code></a>, que es como un plugin <em>le cuenta</em> al anfitrión qué muestra un gestor de archivos de terceros. No se garantiza que siga existiendo: registra dónde estuvo el usuario, y esa carpeta puede haberse borrado o desconectado desde entonces.</td></tr><tr><td><code>PluginSettingsService</code></td><td><code>GetSetting&lt;T&gt;(pluginId, key, defaultValue)</code> — acceso de solo lectura a la configuración persistida propia de un plugin en el almacén de configuración del host. Recurre a tres niveles en cascada: el valor persistido si el usuario alguna vez guardó uno, luego el propio <code>DefaultValue</code> de tu esquema <code>IConfigurable</code> para ese campo si no se persistió nada, y por último el argumento <code>defaultValue</code> que hayas pasado como último recurso — de modo que un valor predeterminado declarado en el esquema es la única fuente de verdad y no necesita una segunda copia codificada a mano en tu punto de llamada. Si guardas en caché un ajuste en lugar de volver a leerlo en cada llamada, suscríbete al evento <code>SettingChanged(pluginId, key)</code> y descarta tu caché cuando se dispare para tu plugin — el host lo lanza justo después de guardar en la página de configuración, que es el único punto fiable para invalidar la caché (una comprobación por pulsación de tecla o por sondeo no verá un cambio hasta lo que sea que lo desencadene coincidentemente la próxima vez, o nunca).</td></tr><tr><td><code>SearchRefreshService</code></td><td><code>RefreshIfMatches(queryMatches)</code> — para un <code>IInstantResultProvider</code> cuyos datos llegan de forma asíncrona (ver <a href="./core-search-actions.html#iinstantresultprovider"><code>IInstantResultProvider</code></a>): una vez que tu obtención en segundo plano termina y has guardado el resultado en caché, llama a esto con un predicado sobre el texto de consulta actual de una búsqueda, y el host vuelve a ejecutar toda búsqueda activa cuya consulta coincida con ese predicado, de modo que el resultado ahora en caché realmente aparezca sin que el usuario necesite volver a escribir nada.</td></tr><tr><td><code>Logger</code></td><td><code>Log(message, level = LogLevel.Info)</code> — escribe en el archivo de registro de la App, visible en <strong>Configuración → Estado del Servicio → App</strong> exactamente igual que las propias líneas de registro del host.</td></tr><tr><td><code>PluginPromptService</code></td><td><code>Prompt(title, fields, initialValues?)</code> — muestra un pequeño modal solicitando los valores de <a href="./abstractions.html#iconfigurable"><code>PluginConfigField</code></a> indicados (el mismo esquema/renderizado de campos que usa el diálogo Configurar de <code>IConfigurable</code>), precargado a partir de <code>initialValues</code> (emparejado por <code>Key</code>) o del propio <code>DefaultValue</code> de cada campo. Devuelve los valores introducidos, indexados por <code>Key</code> de campo, o <code>null</code> si el usuario canceló — estos valores nunca se leen de la configuración persistida real del plugin ni se escriben en ella, así que es seguro reutilizar el esquema de un campo de configuración únicamente para una entrada puntual (por ejemplo, &quot;ponle un nombre antes de añadirlo&quot;) sin tocar el ajuste real que hay detrás.</td></tr></tbody></table><p><code>LogLevel</code> es <code>Error</code> / <code>Warn</code> / <code>Info</code> / <code>Debug</code>, coincidiendo con el filtro de nivel del <a href="./../../user-guide/settings/service-status.html">visor de registros de Estado del Servicio</a>.</p><h2 id="operaciones-de-archivo-del-shell" tabindex="-1">Operaciones de archivo del shell <a class="header-anchor" href="#operaciones-de-archivo-del-shell" aria-label="Permalink to &quot;Operaciones de archivo del shell&quot;">​</a></h2><p><code>SwiftList.PluginSdk.Shell.FileOperations</code>: envoltorios finos sobre el propio <code>IFileOperation</code> del shell de Windows, para que un plugin que mueve archivos produzca el mismo diálogo de progreso, el mismo aviso de &quot;este archivo ya existe&quot; y la misma entrada de deshacer que habría producido el Explorador, en vez de una llamada de <code>System.IO</code> que hace algo sutilmente distinto.</p><table tabindex="0"><thead><tr><th>Ayudante</th><th>Propósito</th></tr></thead><tbody><tr><td><code>ShellPasteHelper</code></td><td><code>PasteAsync(sourcePaths, destinationFolder, move, onCompleted?)</code>: copia (o mueve) cualquier número de rutas a una sola carpeta como <strong>una única</strong> operación del shell, así que una selección múltiple entre unidades sigue produciendo un diálogo y no uno por archivo. Se lanza y vuelve al momento: bloquear a la espera de un diálogo nativo que el usuario puede dejar abierto solo congelaría a quien llamó. <code>onCompleted</code> se dispara cuando la operación termina, tanto si copió todo como si el usuario canceló, porque para una vista que muestra el destino la respuesta a ambos casos es la misma: volver a mirar.</td></tr><tr><td><code>ShellDeleteHelper</code></td><td><code>DeleteAsync(paths, permanent)</code>: envía a la papelera o borra definitivamente, otra vez agrupado en una operación y una confirmación. También se lanza y vuelve.</td></tr><tr><td><code>VirtualFileExtractor</code></td><td><code>HasVirtualFiles(dataObject)</code> / <code>Extract(dataObject, targetFolder)</code>: escribe los archivos que un arrastre trae y que aún no existen en disco: una imagen arrastrada desde un navegador, un adjunto arrastrado desde el correo, un archivo arrastrado desde la vista previa de un zip. Ninguno es una ruta, así que <code>IDataObject.GetData(DataFormats.FileDrop)</code> no encuentra nada; lo que llega es un descriptor con sus nombres más sus bytes de uno en uno por índice, que es lo que esto desempaqueta. Deliberadamente sin filtrar por tipo: rechazar lo que el arrastre estaba dispuesto a entregar exige fiarse de una extensión u olfatear bytes, y ninguna de las dos cosas es asunto de este ayudante. <code>ResolveDestination(folder, name)</code> es esa misma regla de &quot;añadir (2) en vez de sobrescribir&quot;, expuesta para quien escriba los archivos por su cuenta.</td></tr></tbody></table><p>Los dos ayudantes asíncronos corren en el hilo STA propio del SDK (<code>ShellOperationStaWorker</code>, que arranca el host): las interfaces COM del shell exigen uno, y compartirlo evita que un plugin tenga que levantar su propio apartamento.</p>',8)])])}const g=a(r,[["render",i]]);export{m as __pageData,g as default};
