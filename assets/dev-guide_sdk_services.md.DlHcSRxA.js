import{_ as t,o,c as a,a2 as r}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Host Services","description":"","frontmatter":{},"headers":[],"relativePath":"dev-guide/sdk/services.md","filePath":"dev-guide/sdk/services.md","lastUpdated":1785587987000}'),s={name:"dev-guide/sdk/services.md"};function i(d,e,n,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="host-services" tabindex="-1">Host Services <a class="header-anchor" href="#host-services" aria-label="Permalink to &quot;Host Services&quot;">​</a></h1><p>Static services in <code>PluginSdk.Services</code> that expose host-app functionality back to plugins — each is a thin static class wrapping a delegate the host wires up at startup, so plugins call them the same way regardless of what&#39;s running underneath.</p><table tabindex="0"><thead><tr><th>Service</th><th>Purpose</th></tr></thead><tbody><tr><td><code>FuzzyMatchService</code></td><td><code>IsMatch(pattern, text)</code> — whether <code>text</code> (or one of its aliases) matches an fzf-syntax <code>pattern</code>, using the exact same matching the host&#39;s own search uses; <code>GetHighlightMask(text, query)</code> — the per-character highlight mask for that pair, using the same literal/fuzzy/alias fallback tiers (including CJK pinyin) the host&#39;s own results highlight with, so a plugin&#39;s results highlight consistently instead of only handling a literal substring match.</td></tr><tr><td><code>TranslationService</code></td><td><code>Get(key)</code> / <code>Format(key, args)</code> for runtime lookups against the active language; <code>LoadEmbeddedTranslations(assembly, cultureKey, typeName)</code> to load a plugin&#39;s own embedded JSON translations; <code>GetSupportedCultures(assembly)</code>; <code>GetCurrentCulture()</code> — the app&#39;s currently selected UI language (e.g. <code>&quot;zh-CN&quot;</code>), a user setting independent of the OS system locale. Reach for this only when you need the raw culture code itself (e.g. to put in an HTTP <code>Accept-Language</code> header or pick a translation API&#39;s target language) — <code>CultureInfo.CurrentUICulture</code> reflects the OS locale, not this setting, and will silently disagree with it whenever a user&#39;s Windows language and in-app language differ.</td></tr><tr><td><code>IconService</code></td><td><code>GetIcon(path, isDir)</code> and <code>GetThumbnail(path, size)</code> — cached shell icon/thumbnail extraction, so a plugin never has to shell out to the Windows icon APIs itself.</td></tr><tr><td><code>FavoritesService</code></td><td><code>GetFavorites()</code> — read-only access to the user&#39;s <a href="./../../user-guide/settings/favorites.html">Favorites</a> list (<code>FavoriteItem</code>: Name, Path).</td></tr><tr><td><code>HistoryService</code></td><td><code>GetHistoryEntries()</code> — every recorded <a href="./../../user-guide/settings/history.html">History</a> entry, most-recently-opened first, as <code>HistoryEntry { Keyword, Path, Kind, Time }</code> (<code>Kind</code> is a <code>HistoryEntryKind</code>: <code>File</code> / <code>Folder</code> / <code>Application</code>; <code>Keyword</code> is the search text that led to it, empty if opened directly from a Startup Panel tab with no query typed; <code>Time</code> is Unix seconds). Each path appears at most once, under whichever keyword most recently led to it.</td></tr><tr><td><code>FileMetadataService</code></td><td><code>GetMetadataAsync(paths)</code> — batched Size/Created/Modified/Accessed lookup (<a href="./abstractions.html#filemetadata"><code>FileMetadata</code></a>) for a path that <strong>isn&#39;t</strong> already one of your current results — every <code>ISearchResult</code> already carries this via its own <code>Metadata</code> property at no cost (see <a href="./abstractions.html#isearchresult">Abstractions</a>), so reach for this service only for paths you got some other way (e.g. from your own config).</td></tr><tr><td><code>DirectoryIndexerService</code></td><td><code>RegisterDirectory(pluginId, path, recursive, filterPattern)</code> / <code>UnregisterDirectories(pluginId)</code> / <code>SearchDirectoriesAsync(pluginId, query, token)</code> — lets a plugin register its own directories for background indexing and USN monitoring without reimplementing that machinery. Subscribe to the <code>DirectoryChanged</code> event to be told when a registered directory changed on disk (it carries the <code>pluginId</code> the directory was registered under, so ignore the ones that aren&#39;t yours); <code>NotifyDirectoryChanged(pluginId)</code> raises it, which the host does for you when the watcher fires. <code>EnumerateDirectoryAsync(path, recursive, filterPattern, limit, token)</code> lists one directory&#39;s contents from that same index rather than the filesystem — no disk I/O at all for a drive the host indexes, and a live walk for one it doesn&#39;t, so you never have to decide which case applies. It streams, <code>filterPattern</code> selects <strong>files</strong> (directories always come back — drop them by <code>IsDir</code> if unwanted), hidden and system entries are never returned, and <code>limit</code> is worth setting for a recursive listing, since <code>EnumerateDirectoryAsync(@&quot;C:\\&quot;, recursive: true)</code> will deliver every entry on the volume exactly as asked.</td></tr><tr><td><code>PluginSettingsService</code></td><td><code>GetSetting&lt;T&gt;(pluginId, key, defaultValue)</code> — read-only access to a plugin&#39;s own persisted settings from the host&#39;s config store. Falls back through three tiers: the persisted value if the user has ever saved one, then your <code>IConfigurable</code> schema&#39;s own <code>DefaultValue</code> for that field if nothing was persisted, then the <code>defaultValue</code> argument you passed as a last resort — so a schema-declared default is the single source of truth and doesn&#39;t need a second hardcoded copy in your call site. If you cache a setting instead of re-reading it every call, subscribe to the <code>SettingChanged(pluginId, key)</code> event and drop your cache when it fires for your plugin — the host raises it right after a settings-page save, which is the only reliable point to invalidate against (a per-keystroke or polling check will not see a change until whatever coincidentally triggers it next, or never).</td></tr><tr><td><code>SearchRefreshService</code></td><td><code>RefreshIfMatches(queryMatches)</code> — for an <code>IInstantResultProvider</code> whose data arrives asynchronously (see <a href="./core-search-actions.html#iinstantresultprovider"><code>IInstantResultProvider</code></a>): once your background fetch completes and you&#39;ve cached the result, call this with a predicate over a search&#39;s current query text, and the host re-runs every active search that predicate matches so the now-cached result actually appears without the user needing to retype anything.</td></tr><tr><td><code>Logger</code></td><td><code>Log(message, level = LogLevel.Info)</code> — writes to the App&#39;s log file, visible in <strong>Settings → Service Status → App</strong> exactly like the host&#39;s own log lines.</td></tr><tr><td><code>PluginPromptService</code></td><td><code>Prompt(title, fields, initialValues?)</code> — shows a small modal asking for the given <a href="./abstractions.html#iconfigurable"><code>PluginConfigField</code></a> values (the same field schema/rendering <code>IConfigurable</code>&#39;s Configure dialog uses), pre-filled from <code>initialValues</code> (matched by <code>Key</code>) or each field&#39;s own <code>DefaultValue</code>. Returns the entered values keyed by field <code>Key</code>, or <code>null</code> if the user cancelled — these values are never read from or written to the plugin&#39;s actual persisted settings, so it&#39;s safe to reuse a config field&#39;s schema purely for one-off input (e.g. &quot;name this before adding it&quot;) without touching the real setting behind it.</td></tr></tbody></table><p><code>LogLevel</code> is <code>Error</code> / <code>Warn</code> / <code>Info</code> / <code>Debug</code>, matching the level filter in the <a href="./../../user-guide/settings/service-status.html">Service Status log viewer</a>.</p><h2 id="shell-file-operations" tabindex="-1">Shell file operations <a class="header-anchor" href="#shell-file-operations" aria-label="Permalink to &quot;Shell file operations&quot;">​</a></h2><p><code>SwiftList.PluginSdk.Shell.FileOperations</code> — thin wrappers over the Windows shell&#39;s own <code>IFileOperation</code>, so a plugin that moves files around gets the same progress dialog, the same &quot;this file already exists&quot; prompt and the same undo entry Explorer would have produced, rather than a <code>System.IO</code> call that silently does something subtly different.</p><table tabindex="0"><thead><tr><th>Helper</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ShellPasteHelper</code></td><td><code>PasteAsync(sourcePaths, destinationFolder, move, onCompleted?)</code> — copies (or moves) any number of paths into one folder as a <strong>single</strong> shell operation, so a cross-drive multi-select still produces one dialog rather than one per file. Fire-and-forget: it returns immediately, because blocking on a native dialog the user may leave open would freeze your caller. <code>onCompleted</code> fires when the operation is over — whether it copied everything or the user cancelled, since a view showing the destination has the same answer to both, which is to go and look again.</td></tr><tr><td><code>ShellDeleteHelper</code></td><td><code>DeleteAsync(paths, permanent)</code> — recycles or permanently deletes, again batched into one operation and one confirmation. Also fire-and-forget.</td></tr><tr><td><code>VirtualFileExtractor</code></td><td><code>HasVirtualFiles(dataObject)</code> / <code>Extract(dataObject, targetFolder)</code> — writes out the files a drag is carrying that do not exist on disk yet: an image dragged from a browser, an attachment dragged from a mail client, a file dragged out of a zip preview. None of those arrive as a path, so <code>IDataObject.GetData(DataFormats.FileDrop)</code> finds nothing; what arrives is a descriptor naming them plus their bytes one index at a time, which is what this unpacks. Deliberately unfiltered by type: refusing what a drag was willing to hand over means trusting an extension or sniffing bytes, and neither is this helper&#39;s business. <code>ResolveDestination(folder, name)</code> is the same &quot;add (2) rather than overwrite&quot; naming it uses, exposed for callers writing files out themselves.</td></tr></tbody></table><p>Both of the async helpers run on the SDK&#39;s own STA worker thread (<code>ShellOperationStaWorker</code>), which the host starts — the shell&#39;s COM interfaces require one, and sharing it means a plugin never has to spin up an apartment of its own.</p>',8)])])}const p=t(s,[["render",i]]);export{g as __pageData,p as default};
