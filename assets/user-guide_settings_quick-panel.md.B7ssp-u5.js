import{_ as t,o,c as s,a2 as a}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Quick Panel","description":"","frontmatter":{},"headers":[],"relativePath":"user-guide/settings/quick-panel.md","filePath":"user-guide/settings/quick-panel.md","lastUpdated":1785587205000}'),n={name:"user-guide/settings/quick-panel.md"};function i(r,e,l,h,d,c){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="quick-panel" tabindex="-1">Quick Panel <a class="header-anchor" href="#quick-panel" aria-label="Permalink to &quot;Quick Panel&quot;">​</a></h1><p>A floating panel summoned by a hotkey and docked into the bottom-right corner of whatever window is in front, at half the height and half the width of it. It shows folders you nominate — as thumbnail tiles or as a list — so files can be reached, dragged out of, or dropped into without leaving the window you are working in. Drag its top edge to move it somewhere else for the current summon.</p><ul><li><strong>Enable the quick panel</strong> — master switch; off means the hotkey does nothing.</li></ul><p>The key that summons it is on the <a href="./hotkeys-page.html">Hotkeys</a> page (<code>Ctrl+F2</code> by default).</p><h2 id="workspaces" tabindex="-1">Workspaces <a class="header-anchor" href="#workspaces" aria-label="Permalink to &quot;Workspaces&quot;">​</a></h2><p>The panel shows one <strong>workspace</strong> at a time, and every workspace is one tab in its tab strip. A workspace is a set of sources assembled for one kind of work — a project&#39;s folders, a place you keep reference material, an inbox you drop things into.</p><p>The left-hand list is the workspaces themselves, with <strong>New workspace</strong>, <strong>Duplicate workspace</strong> and <strong>Delete workspace</strong> buttons, and the same up/down-arrow (or drag-to-reorder) list used elsewhere in Settings (see <a href="./favorites.html">Favorites</a>). Top to bottom here is left to right in the panel&#39;s tab strip.</p><ul><li><strong>Name</strong> — what its tab is labelled. Left empty, it falls back to a translated default, so a never-renamed workspace follows the UI language.</li><li><strong>Enabled</strong> — the checkbox beside each workspace. Off keeps the workspace configured but gives it no tab, for one set up for work you are not doing this month, where deleting it would mean rebuilding the source list to get it back. The <strong>×</strong> on a tab in the live panel does exactly this, which is why turning it back on is done here.</li></ul><p>The selected workspace is edited through three sub-tabs: <strong>Sources</strong>, <strong>Plugin sources</strong>, and <strong>Apps</strong>.</p><h2 id="sources" tabindex="-1">Sources <a class="header-anchor" href="#sources" aria-label="Permalink to &quot;Sources&quot;">​</a></h2><p>Each source is one group in the panel, shown in the order this list is in. <strong>Add folder</strong> picks one; the checkbox beside a row hides that group without removing it; the name box overrides the group&#39;s heading (leave it empty for the folder&#39;s own name); and <strong>More options</strong> opens the rest:</p><ul><li><strong>Show</strong> — what the group draws from the folder: <ul><li><strong>Recently changed files</strong> — only what changed recently, newest first, answered from the index rather than by walking the folder.</li><li><strong>Everything, newest first</strong> — never hides a file on age, only decides what comes first.</li><li><strong>Everything, by name</strong> — a folder used as a shortcut bar.</li></ul></li><li><strong>Folder</strong> — the folder itself, with a <strong>…</strong> button to browse for it.</li><li><strong>Include subfolders</strong> — off by default.</li><li><strong>Accept dropped files</strong> — files and folders dragged onto this group are copied into its folder, using Windows&#39; own file copy (its progress dialog, its conflict prompts, its undo). Always a copy, never a move. Off by default and asked per source: a folder kept as an inbox wants it, and one you only ever read from does not.</li><li><strong>Files</strong> — one or more patterns separated by <code>;</code> or <code>,</code> (e.g. <code>*.mp4;*.mkv</code>). Folders are always shown.</li><li><strong>At most</strong> — how many entries the group shows. 0 means everything the source has.</li><li><strong>Changed within (minutes)</strong> — only entries changed within this long qualify. 0 means no age limit.</li><li><strong>Show as list</strong> — the group opens as a detail list rather than thumbnail tiles. Which suits a group is a property of the folder: images want tiles, documents want names and dates. Tiles divide the row between five, so a panel docked to a wide window gets thumbnails worth looking at rather than more of them — more than five once even five would be wider than a thumbnail can fill, fewer than five on a panel too narrow for that many legible ones. Whatever the count, the row is divided rather than filled with fixed-size tiles, so it never ends in a gap. Each picture keeps its own shape, so a folder of 16:9 video does not become a grid of tall boxes with a band of empty tile in each.</li></ul><p>A folder added here starts on <strong>Everything, by name</strong> — a folder picked by hand is nearly always a place things are kept. The workspace a fresh install comes with is the exception, and deliberately so: Desktop, Downloads and Documents are places things arrive, and start on recently-changed files.</p><h2 id="plugin-sources" tabindex="-1">Plugin sources <a class="header-anchor" href="#plugin-sources" aria-label="Permalink to &quot;Plugin sources&quot;">​</a></h2><p>Sources provided by plugins. Tick one to add it to this workspace; it then appears in the <strong>Sources</strong> list beside the folders and is ordered, renamed and hidden exactly like one. CoreExtensions ships three: <strong>Windows Recent Items</strong> (the shell&#39;s own recent-documents list, resolved to the files it points at, newest first), <strong>History</strong> (what you opened through SwiftList itself, most recent first — a different list, and the only one of the two that includes applications) and <strong>Favorites</strong> (your <a href="./favorites.html">Favorites</a>, in the order you arranged them).</p><p>Adding one is a decision made once, which is why it is a separate tab from the list where ordering and renaming happen. Only sources whose plugin component is enabled under <a href="./plugins.html">Plugins</a> are listed. An id whose plugin is gone keeps its place rather than being pruned, so a plugin switched off for a week comes back where you put it.</p><h2 id="apps" tabindex="-1">Apps <a class="header-anchor" href="#apps" aria-label="Permalink to &quot;Apps&quot;">​</a></h2><p>Applications this workspace belongs to, one process name per line (<code>chrome</code> or <code>chrome.exe</code>, either way). Summon the panel over one of them and it opens on this workspace instead of wherever it was left — the app you are already in says which set of folders you mean. Left empty, the workspace is only ever reached by hand.</p><h2 id="quick-panel-only" tabindex="-1">Quick panel only <a class="header-anchor" href="#quick-panel-only" aria-label="Permalink to &quot;Quick panel only&quot;">​</a></h2><p>Applications the panel stays out of, one process name per line. Added on top of the global <a href="./hotkeys-page.html#process-blacklist">process blacklist</a> rather than replacing it: whatever is blocked globally is blocked here too. This list is for apps only this panel has a reason to avoid — it docks itself onto the window in front, so a full-screen player or a game is ruined by it without deserving a global block.</p><h2 id="using-the-panel" tabindex="-1">Using the panel <a class="header-anchor" href="#using-the-panel" aria-label="Permalink to &quot;Using the panel&quot;">​</a></h2><ul><li><strong>Filter box</strong> — right of the tab strip, focused the moment the panel opens. It matches fuzzily (the same fzf-style matching the search window uses, pinyin aliases included) against the current workspace only. A group with nothing left matching is hidden while the filter stands.</li><li><strong>Enter</strong> opens whatever is selected, which is also what a double-click does. The first entry is selected from the start, so a summon can be answered with type-then-Enter without ever leaving the filter box.</li><li><strong>Up/Down</strong> move the selection, and cross from one group into the next rather than stopping at a group&#39;s last row — the groups read as one list top to bottom. They work from the filter box without taking the keyboard out of it, so narrowing and picking are the same gesture. Inside a group shown as tiles, they still move a row at a time as the grid is laid out. The configured &quot;select next/previous item&quot; hotkeys (<code>Ctrl+N</code>/<code>Ctrl+P</code> by default) do the same, unless a plugin action is bound to one of them, which wins.</li><li><strong>Switching workspaces</strong> — hold the &quot;jump to Nth result&quot; modifier (<code>Ctrl</code> by default, see <a href="./../hotkeys.html">Hotkeys</a>) and press 1–9, or click a tab. Tabs drag to reorder, and each has an <strong>×</strong> that turns that workspace off. Closing the last one closes the panel.</li><li><strong>Group headings</strong> carry a sort toggle (by name / by date modified), a view toggle (tiles / list) and a collapse arrow. What you do to a group here lasts for as long as the panel is open; the starting state is what the settings above say.</li><li><strong>Selecting several</strong> — in tile view, drag a box across empty space to rubber-band a set. A selection belongs to one group, since each group draws its own list; clicking empty space clears it.</li><li><strong>Dropping files in</strong> — drag files, folders, or an image straight off a web page onto a group that accepts drops. Anything a drag can offer as a file is taken, not just images. The group reloads itself once the copy finishes.</li><li><strong>Preview</strong> — the QuickLook key (<code>Alt+P</code> by default, rebindable on the <a href="./hotkeys-page.html">Hotkeys</a> page like any other) opens the preview window for the selected file, and it follows the selection from then on. It docks to the right of the panel, or to its left when the screen edge leaves no room there — which is what a panel in the bottom-right corner of a maximized window gets. Clicking into the preview does not dismiss the panel; clicking away from both does. It closes with the panel.</li><li><strong>Keeping it open</strong> — the panel closes when it loses focus. The pin button, or the &quot;keep window open&quot; hotkey (<code>Ctrl+T</code> by default, the same one the quick window uses), suspends that for the current summon.</li><li><strong>Escape</strong> clears the filter box if it has text, and closes the panel otherwise.</li><li>The panel keeps itself current: a folder it is showing that changes on disk is reloaded, through the same index-backed watching the rest of the app uses rather than a scan of its own.</li></ul>',22)])])}const u=t(n,[["render",i]]);export{g as __pageData,u as default};
