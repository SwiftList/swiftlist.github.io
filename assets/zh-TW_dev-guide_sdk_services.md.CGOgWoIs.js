import{_ as t,o as d,c as o,a2 as c}from"./chunks/framework.BWuWLRhz.js";const p=JSON.parse('{"title":"宿主服務","description":"","frontmatter":{},"headers":[],"relativePath":"zh-TW/dev-guide/sdk/services.md","filePath":"zh-TW/dev-guide/sdk/services.md","lastUpdated":1785587987000}'),r={name:"zh-TW/dev-guide/sdk/services.md"};function a(i,e,s,l,n,h){return d(),o("div",null,[...e[0]||(e[0]=[c('<h1 id="宿主服務" tabindex="-1">宿主服務 <a class="header-anchor" href="#宿主服務" aria-label="Permalink to &quot;宿主服務&quot;">​</a></h1><p><code>PluginSdk.Services</code> 裡的靜態服務，把宿主應用程式的功能公開給外掛——每一個都是包裝了宿主啟動時接好的委派的薄靜態類別，所以不管底層實際執行的是什麼，外掛的呼叫方式都一樣。</p><table tabindex="0"><thead><tr><th>服務</th><th>用途</th></tr></thead><tbody><tr><td><code>FuzzyMatchService</code></td><td><code>IsMatch(pattern, text)</code> —— <code>text</code>(或它的某個別名)是否符合 fzf 語法的 <code>pattern</code>,用的是和宿主自身搜尋完全一致的比對邏輯;<code>GetHighlightMask(text, query)</code> —— 對應這一對 (text, query) 的逐字元高亮遮罩,用的是宿主自己那套字面量/模糊/別名多層保底演算法(含中文拼音),這樣外掛自己結果的高亮就能和其他結果保持一致,而不是只能處理簡單的字面量子字串比對。</td></tr><tr><td><code>TranslationService</code></td><td><code>Get(key)</code> / <code>Format(key, args)</code> 在執行階段按目前語言查詢;<code>LoadEmbeddedTranslations(assembly, cultureKey, typeName)</code> 載入外掛自己內嵌的 JSON 語言包;<code>GetSupportedCultures(assembly)</code>;<code>GetCurrentCulture()</code> —— 應用程式目前選定的介面語言(比如 <code>&quot;zh-CN&quot;</code>),這是一個獨立於作業系統語言的使用者設定。只有你確實需要拿到原始語言代碼本身時才用它(比如塞進 HTTP 的 <code>Accept-Language</code> 要求標頭,或者決定翻譯 API 的目標語言)——<code>CultureInfo.CurrentUICulture</code> 反映的是作業系統的語言,不是這個設定,一旦使用者的 Windows 語言和應用程式內語言不一致,兩者就會悄悄對不上。</td></tr><tr><td><code>IconService</code></td><td><code>GetIcon(path, isDir)</code> 和 <code>GetThumbnail(path, size)</code> —— 帶快取的 Shell 圖示/縮圖擷取，外掛不需要自己呼叫 Windows 圖示 API。</td></tr><tr><td><code>FavoritesService</code></td><td><code>GetFavorites()</code> —— 唯讀存取使用者的<a href="./../../user-guide/settings/favorites.html">我的最愛</a>清單(<code>FavoriteItem</code>:Name、Path)。</td></tr><tr><td><code>HistoryService</code></td><td><code>GetHistoryEntries()</code> —— 每一條已記錄的<a href="./../../user-guide/settings/history.html">歷史記錄</a>項目,按最近開啟優先排序,型別是 <code>HistoryEntry { Keyword, Path, Kind, Time }</code>(<code>Kind</code> 是 <code>HistoryEntryKind</code>:<code>File</code> / <code>Folder</code> / <code>Application</code>;<code>Keyword</code> 是開啟時輸入框裡的搜尋文字,沒打字直接從起始面板點開的話就是空字串;<code>Time</code> 是 Unix 秒)。同一個路徑最多只會出現一次,歸屬於最近一次帶它進來的那個關鍵字。</td></tr><tr><td><code>FileMetadataService</code></td><td><code>GetMetadataAsync(paths)</code> —— 批次查詢 Size/Created/Modified/Accessed(<a href="./abstractions.html#filemetadata"><code>FileMetadata</code></a>)，用於查詢<strong>不屬於</strong>你目前結果集的路徑——每個 <code>ISearchResult</code> 本身就透過自己的 <code>Metadata</code> 屬性免費攜帶這些資料(參見<a href="./abstractions.html#isearchresult">共用抽象契約</a>)，所以只有拿到的路徑不是來自結果物件(比如來自你自己的設定)時才需要用這個服務。</td></tr><tr><td><code>DirectoryIndexerService</code></td><td><code>RegisterDirectory(pluginId, path, recursive, filterPattern)</code> / <code>UnregisterDirectories(pluginId)</code> / <code>SearchDirectoriesAsync(pluginId, query, token)</code> —— 讓外掛註冊自己的目錄進行背景索引和 USN 監看，而不用自己重新實作這套機制。訂閱 <code>DirectoryChanged</code> 事件即可在已註冊目錄發生磁碟變化時收到通知(事件帶的是註冊時用的 <code>pluginId</code>，不是你的就忽略)；<code>NotifyDirectoryChanged(pluginId)</code> 用來觸發它，監看器觸發時宿主會替你呼叫。<code>EnumerateDirectoryAsync(path, recursive, filterPattern, limit, token)</code> 從同一份索引(而不是檔案系統)列出某個目錄的內容——宿主已索引的磁碟完全不產生磁碟 I/O，沒索引的目錄則自動改為即時走訪，呼叫方不需要自己判斷屬於哪種情況。它是串流式的；<code>filterPattern</code> 篩的是<strong>檔案</strong>(資料夾一律回傳，不需要就按 <code>IsDir</code> 過濾)；隱藏和系統項目永遠不回傳；遞迴列舉時值得設 <code>limit</code>——<code>EnumerateDirectoryAsync(@&quot;C:\\&quot;, recursive: true)</code> 會老老實實把整個磁碟區的每一筆都交給你。</td></tr><tr><td><code>PluginSettingsService</code></td><td><code>GetSetting&lt;T&gt;(pluginId, key, defaultValue)</code> —— 從宿主的設定儲存區裡唯讀存取外掛自己持久化的設定。回退分三層:使用者存過就用持久化的值;沒存過就用你 <code>IConfigurable</code> schema 裡該欄位自己宣告的 <code>DefaultValue</code>;兩者都沒有才輪到你傳進來的 <code>defaultValue</code> 保底——這樣 schema 裡宣告的預設值就是唯一權威來源,呼叫方不需要在程式碼裡再手寫一份重複的預設值。如果你把某個設定快取了起來而不是每次都重新讀取,記得訂閱 <code>SettingChanged(pluginId, key)</code> 事件,在它為你的外掛觸發時清空快取——宿主是在設定頁儲存之後立刻觸發這個事件的,這是唯一可靠的失效時機(不管是按鍵觸發還是輪詢檢查,都要等到別的什麼東西湊巧觸發了才會看到變化,或者乾脆永遠看不到)。</td></tr><tr><td><code>SearchRefreshService</code></td><td><code>RefreshIfMatches(queryMatches)</code> —— 給資料是非同步到達的 <code>IInstantResultProvider</code> 用的(參見 <a href="./core-search-actions.html#iinstantresultprovider"><code>IInstantResultProvider</code></a>):等你的背景要求完成、結果也快取好之後，呼叫這個方法並傳入一個基於目前查詢文字的判斷函式，宿主會把所有符合這個判斷的、正在進行的搜尋重新跑一遍，這樣剛快取好的結果就能直接顯示出來，不需要使用者重新輸入。</td></tr><tr><td><code>Logger</code></td><td><code>Log(message, level = LogLevel.Info)</code> —— 寫入 App 的記錄檔，和宿主自己的記錄行一樣，顯示在<strong>設定 → 執行狀態 → App</strong> 裡。</td></tr><tr><td><code>PluginPromptService</code></td><td><code>Prompt(title, fields, initialValues?)</code> —— 彈出一個小的強制回應視窗，向使用者詢問給定<a href="./abstractions.html#iconfigurable"><code>PluginConfigField</code></a>欄位的值(用的正是 <code>IConfigurable</code> 的設定對話方塊那套欄位 schema/繪製邏輯)，按 <code>Key</code> 比對從 <code>initialValues</code> 預先填入，沒有就用各欄位自己的 <code>DefaultValue</code>。回傳按欄位 <code>Key</code> 索引的填寫結果，使用者取消則回傳 <code>null</code>——這些值不會讀取或寫入外掛真正持久化的設定，所以可以放心重複使用某個設定欄位的 schema 單純做一次性輸入(比如「新增前先給它取個名字」)，不會碰到背後真實的那個設定項目。</td></tr></tbody></table><p><code>LogLevel</code> 是 <code>Error</code> / <code>Warn</code> / <code>Info</code> / <code>Debug</code>，與<a href="./../../user-guide/settings/service-status.html">執行狀態記錄檢視器</a>裡的層級篩選器一致。</p><h2 id="shell-檔案操作" tabindex="-1">Shell 檔案操作 <a class="header-anchor" href="#shell-檔案操作" aria-label="Permalink to &quot;Shell 檔案操作&quot;">​</a></h2><p><code>SwiftList.PluginSdk.Shell.FileOperations</code> —— 對 Windows shell 自己的 <code>IFileOperation</code> 的一層薄封裝。外掛搬動檔案時，使用者看到的是和檔案總管一模一樣的進度對話方塊、「檔案已存在」提示和復原記錄，而不是一次行為略有不同的 <code>System.IO</code> 呼叫。</p><table tabindex="0"><thead><tr><th>輔助類別</th><th>用途</th></tr></thead><tbody><tr><td><code>ShellPasteHelper</code></td><td><code>PasteAsync(sourcePaths, destinationFolder, move, onCompleted?)</code> —— 把任意多個路徑複製(或移動)進同一個資料夾，合併成<strong>一次</strong> shell 操作，所以跨磁碟的多選也只跳一個對話方塊，而不是每個檔案一個。發出即返回：native 對話方塊可能被使用者晾在那裡，阻塞呼叫方只會把介面凍住。<code>onCompleted</code> 在操作結束時觸發——不管是複製完了還是使用者取消了，因為對一個正顯示目標資料夾的檢視來說，這兩種情況的應對是同一個：回去重新看一眼。</td></tr><tr><td><code>ShellDeleteHelper</code></td><td><code>DeleteAsync(paths, permanent)</code> —— 放進資源回收筒或永久刪除，同樣合併成一次操作、一次確認。同樣是發出即返回。</td></tr><tr><td><code>VirtualFileExtractor</code></td><td><code>HasVirtualFiles(dataObject)</code> / <code>Extract(dataObject, targetFolder)</code> —— 把拖動攜帶的、磁碟上還不存在的檔案寫出來：從瀏覽器拖出的圖片、從郵件用戶端拖出的附件、從壓縮檔預覽裡拖出的檔案。它們都不是路徑，所以 <code>IDataObject.GetData(DataFormats.FileDrop)</code> 什麼也拿不到；真正到來的是一份列出檔名的描述元，加上按索引一次給一個的位元組流，這個類別做的就是把它拆出來。刻意不按類型過濾：拒絕拖動方願意交出來的東西，意味著要麼信副檔名、要麼嗅探位元組，這兩件事都不該由它來做。<code>ResolveDestination(folder, name)</code> 是它「重名就加 (2) 而不是覆寫」的那套命名規則，單獨暴露出來給自己寫檔案的呼叫方用。</td></tr></tbody></table><p>兩個非同步輔助類別都跑在 SDK 自己的 STA 工作執行緒上(<code>ShellOperationStaWorker</code>，由宿主啟動)——shell 的 COM 介面要求 STA，共用一條意味著外掛不必自己開一個套間。</p>',8)])])}const g=t(r,[["render",a]]);export{p as __pageData,g as default};
